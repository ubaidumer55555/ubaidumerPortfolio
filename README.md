# Ubaid Umer Portfolio

Modern React portfolio built with Vite + React 19.

## Scripts

- `npm start` / `npm run dev` — local development (http://localhost:3000)
- `npm run build` — production build → `build/`
- `npm run preview` — preview the production build
